package woowacourse.movie.common

import woowacourse.movie.list.model.Advertisement

object AdvertisementDataSource {
    val advertisementList =
        listOf(
            Advertisement(),
        )
}
